# Maintainer: Jan Boelsche <jan@lagomorph.de>

pkgname='plymouth-tty7'
pkgver=1.0
pkgrel=1
pkgdesc="Modifies plymouth's initcpio hook to start plymouth on tty7 (for a smooth transition to X)"
packager='Jan Boelsche'
arch=('any')
license=('GPL')
groups=()
depends=('plymouth' 'sed')
makedepends=()
checkdepends=()
optdepends=()
install=${pkgname}.install
